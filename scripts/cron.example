# Example cron entry: kick off the LangGraph orchestrator once per day at 01:00.
# Ensure your virtualenv or system Python has project deps installed.
0 1 * * * cd /path/to/lead_generation-main && /usr/bin/env python scripts/run_orchestrator.py --tenant-id 1 --input "run full pipeline" >> /var/log/leadgen-orchestrator.log 2>&1

# Optional: apply app migrations on deploy/startup
# @reboot cd /path/to/lead_generation-main && /usr/bin/env python scripts/run_app_migrations.py >> /var/log/leadgen-migrations.log 2>&1
